import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    cardProduto: {
        backgroundColor: "#f3f3f3",
        marginBottom: 20,
        padding: 20,
        borderRadius: 16
    },
    title: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 8,
    },
    impressoes: {
        fontSize: 14,
        marginBottom: 20,
    }
});
